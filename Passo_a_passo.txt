GITHUB
## Depois apenas repetição dos comandos:
* git status (para ver o que foi modificado)
* git add -A ou git add . (para adicionar todos novamente)
* git commit -m "Mensagem" (para dar commit no github)
* git push -u origin main (para upar tudo no github)
Prefira mensagens como “implementa X” ou “corrige Y”, em vez de “X foi implementado” ou “Y foi corrigido”.

6 CRITÉRIOS DE AVALIAÇÃO
NOTA:
1. O repositório do projeto apresenta branches e commits distintos e nomeadas padronizadamente para cada funcionalidade desenvolvida. 👍
2. O repositório do projeto apresenta os arquivos (.sql, .csv, .py e README.md) estruturados conforme as instruções. 👍
3. Realizou a extração automatizada (download + carga), com tratamento de exceções (try/except) e sem duplicar registros em reexecuções 
4. Todos os campos estão corretamente convertidos, sem inconsistências
5. Apresentou a criação de uma tabela agregada por meio da junção de tabelas (JOIN) e agrupamento de dados (GROUP BY), refl etindo 
as regras de negócio estipuladas
6. As perguntas de negócio foram pertinentes e respondidas com análises baseadas em evidências extraídas do tratamento 
e visualização do conjunto de dados fornecido.
7. As visualizações gráfi cas apresentam a identifi cação completa de seus elementos estruturais (título, eixos nomeados e legenda), 
refl etindo os dados extraídos do projeto.
8. O script de criação da tabela apresenta a defi nição explícita de chaves primárias (PK), chaves estrangeiras 
(FK) e restrições (constraints), assegurando a integridade referencial entre as tabelas.